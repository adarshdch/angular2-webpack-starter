export class Donor {
  constructor(
    
    
    public firstName: string,
    public lastName: string,
    public email: string, 
    public contactNumber: string, 
    public bloodGroup: string, 
    public lat: number,
    public long: number
  ) {  }
}